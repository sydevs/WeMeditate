
= translate("devise.mailer.invitation_instructions.hello", email: @resource.name)
= translate("devise.mailer.invitation_instructions.someone_invited_you", url: Rails.configuration.locale_hosts[I18n.locale])
= accept_invitation_url(@resource, invitation_token: @token)

- if @resource.invitation_due_at
  = translate("devise.mailer.invitation_instructions.accept_until", due_date: l(@resource.invitation_due_at, format: :'devise.mailer.invitation_instructions.accept_until_format'))

= strip_tags translate("devise.mailer.invitation_instructions.ignore")
