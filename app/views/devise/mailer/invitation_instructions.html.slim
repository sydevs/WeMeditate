
p = translate("devise.mailer.invitation_instructions.hello", email: @resource.name)
p = translate("devise.mailer.invitation_instructions.someone_invited_you", url: Rails.configuration.locale_hosts[Globalize.locale])
- invitation_url = accept_invitation_url(@resource, invitation_token: @token)
p = link_to translate('devise.mailer.invitation_instructions.accept'), invitation_url
- if @resource.invitation_due_at
  p = translate('devise.mailer.invitation_instructions.accept_until', due_date: l(@resource.invitation_due_at, format: :'devise.mailer.invitation_instructions.accept_until_format'))

p = translate('devise.mailer.invitation_instructions.ignore').html_safe
