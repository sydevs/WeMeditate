
Application.elements.form.map(function(form) {
  if (form.type == 'contact') {
    form.setMessage('<%= @message %>', '<%= @success ? "positive" : "negative" %>')
    form.setLoadingState(false)
  }
})
