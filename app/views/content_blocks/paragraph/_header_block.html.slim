
ruby:
  classes = %w[cb-header-paragraph]
  classes << 'cb-header-paragraph--centered' if block.dig(:decorations, :leaves)
  tag = block[:level] || :h2

= content_tag tag, class: classes do
  .cb-header-paragraph__anchor id=block[:text].parameterize
  = block[:text]
