ruby:
  allow = policy(record)

= f.input :name

- if allow.update_structure?
  = f.input :icon, wrapper: :semantic_file, icon: :image, file: record.icon

- if record.icon.present?
  .preview-item = image_tag record.icon.url, class: 'ui tiny image'
